version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ticket-system:latest
    container_name: ticket-system-container
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
